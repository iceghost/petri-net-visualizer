const config = {
  mode: "jit",
  purge: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    fontFamily: {
      body: ["Hepta Slab", "sans-serif"]
    },
    extend: {},
  },

  plugins: [],
};

module.exports = config;
